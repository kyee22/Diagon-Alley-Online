<div id="<%= dom_id category %>">
  <p>
    <strong>Name:</strong>
    <%= category.name %>
  </p>

  <p>
    <b>Recipes of this category:</b>
    <% if category.recipes.any? %>
      <%= (category.recipes.collect { |recipe| recipe.title }).join(" / ") %>
    <% else %>
      <span style="color: #999;">No recipes available in this category (ง •̀_•́)ง</span>
    <% end %>
  </p>
</div>
